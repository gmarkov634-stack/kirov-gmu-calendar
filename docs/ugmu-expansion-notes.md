# UGMU expansion branch

This branch keeps UGMU fail-closed while source discovery, parser shapes, QA, regression and production readiness are implemented.

Current completed scope:
- university scaffold (`active=false`)
- official source registry
- medicine source discovery adapter and CLI
- source discovery tests

Publishing, checkout redirects and automatic semantic parsing remain disabled.
