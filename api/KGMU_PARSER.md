# КГМУ: серверный парсер и очередь ручной проверки

## Назначение

Слой внедряет автоматический разбор XLSX Кировского ГМУ в тот же API, который обслуживает семестровые и годовые подписки.

Главный принцип: **новый XLSX не заменяет опубликованное расписание, пока парсер и QA не подтвердили результат**. Неизвестная структура, неоднозначность, несовпадение учебного периода или ошибка QA создают `REVIEW_REQUIRED`; текущие календари покупателей остаются прежними.

## Поток данных

```text
XLSX КГМУ
  -> structural XLSX reader (cells + native merged ranges)
  -> classifier R / C / S / UNKNOWN
  -> parser staging
  -> QA
  -> PASS -> READY_TO_PUBLISH
       -> manual publish (default)
       -> automatic publish only when KGMU_AUTO_PUBLISH=true
  -> REVIEW_REQUIRED -> parser-reviews/... + Telegram владельцу
```

## Статус типов

- `R` — недельная сетка по дням недели и группам: **включён**.
- `C` — цикловая сетка старших курсов: код парсера и unit fixture существуют, но production ingest пока оставляет тип заблокированным до полной проверки на эталонном исходном XLSX.
- `S` — смешанная недельно-цикловая структура: пока `PARSER_S_NOT_ENABLED`.
- `UNKNOWN` — неизвестная сигнатура; автоматическая публикация запрещена.

Классификатор проверен на реальных исходных КГМУ и распознаёт:

```text
1_lech._1potok-16-03-2026-13.xlsx              -> R
4_kurs_lechebnyy_fakultet-02-02-2026-14.xlsx -> C
2_stomat-07-05-2026-15.xlsx                   -> S
```

## Верификация R

Парсер `R` сверялся с уже вручную проверенным расписанием лечебного факультета, группы 101–110, второй семестр 2025/26.

Контрольный результат:

- 119/119 исходных учебных anchor-блоков покрыты;
- 2654 события;
- по группам: `101=265, 102=265, 103=266, 104=266, 105=266, 106=265, 107=264, 108=265, 109=266, 110=266`;
- 0 необработанных блоков;
- 0 необъяснённых временных пересечений после правил замещения;
- проверки дополнительных занятий из примечаний проходят;
- полное сравнение с эталонным ICS дало 0 отсутствующих и 0 лишних событий по группе, названию, дате, времени, месту и форме аттестации.

Парсер сохраняет происхождение события (`sourceCell`, `sourceRange`, SHA-256 XLSX), поэтому QA может связывать нормализованное событие с исходной ячейкой.

## Хранилище

В том же S3/Object Storage используются отдельные зоны:

```text
parser-staging/kgmu/sources/<sha256>/<filename>.xlsx
parser-staging/kgmu/normalized/<sha256>.json
parser-reviews/kgmu/<review-id>.json
schedules/...                                      # только опубликованные расписания
```

Staging и review сами по себе не меняют выдачу действующих подписок.

## Проверка учебного периода

Учебный год и семестр извлекаются из самого XLSX. Параметры запроса ingest сверяются с источником. Например, весенний файл нельзя случайно опубликовать как осенний: такой запрос получает `PERIOD_MISMATCH` и блокируется.

Поддерживаются формы учебного года `2025/26`, `2025-2026`, `2026/27` и эквивалентные записи.

## Admin API

Все маршруты требуют `X-Admin-Token`.

### Загрузить XLSX

```text
POST /api/v1/admin/kgmu/ingest?filename=1_lech.xlsx&program=medicine&course=1&academicYear=2025%2F26&semester=2
Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
X-Admin-Token: <ADMIN_TOKEN>

<binary XLSX body>
```

Для `R` с успешным QA по умолчанию ответ получает статус `READY_TO_PUBLISH`. Для `C`, `S`, `UNKNOWN` и QA-fail — `REVIEW_REQUIRED`.

### Опубликовать проверенный staging

```text
POST /api/v1/admin/parser-reviews/<review-id>/publish
X-Admin-Token: <ADMIN_TOKEN>
```

Публикация разрешается только для `READY_TO_PUBLISH`, если сохранённый normalized JSON имеет тот же SHA-256 исходника и `QA=PASS`. Затем расписания записываются в versioned и flat `schedules/...` keys, которые читает subscription API.

### Список review

```text
GET /api/v1/admin/parser-reviews?status=REVIEW_REQUIRED&limit=100
X-Admin-Token: <ADMIN_TOKEN>
```

Можно также запрашивать `status=READY_TO_PUBLISH` или `status=PUBLISHED`.

### Одна карточка

```text
GET /api/v1/admin/parser-reviews/<review-id>
X-Admin-Token: <ADMIN_TOKEN>
```

## Telegram

Переменные контейнера:

```text
TELEGRAM_BOT_TOKEN=<bot token>
TELEGRAM_ADMIN_CHAT_ID=<chat id владельца>
KGMU_XLSX_MAX_BYTES=26214400
KGMU_AUTO_PUBLISH=false
```

`KGMU_AUTO_PUBLISH` по умолчанию выключен. При `REVIEW_REQUIRED` Telegram получает review ID, файл, период, тип и причину. Если Telegram не настроен или временно недоступен, карточка review всё равно остаётся в Object Storage.

## Семестровая и годовая подписка

Семестровая ссылка остаётся привязана к купленному семестру и получает только опубликованное расписание этого периода.

Годовая ссылка объединяет опубликованные `semester-1` и `semester-2` одного учебного года. Если на момент покупки опубликован только первый семестр, ссылка работает с ним; после публикации второго он автоматически появляется в той же ICS-ссылке при обновлении календаря — новый токен и повторный импорт не требуются.

## Следующие этапы

1. Полностью сверить `C` с эталонным расписанием 401–420: 2230 событий, 220/220 блоков и два разрешённых C13-конфликта.
2. После полного совпадения подключить `C` к тому же staging/QA/publish pipeline.
3. Перенести и сверить `S01–S07` на стоматологии 291–294: 830 событий и 62/62 блока.
4. Подключить мониторинг официального сайта КГМУ: новый XLSX должен автоматически попадать в ingest, но публикация остаётся fail-closed.
