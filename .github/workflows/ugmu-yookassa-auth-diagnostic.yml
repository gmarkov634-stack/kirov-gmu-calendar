name: UGMU YooKassa auth diagnostic (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve superseded auth diagnostic
        run: |
          echo "This early YooKassa auth diagnostic is retired."
          echo "It treated the container secret reference as a direct Basic-auth secret and was superseded by Secret Manager aware read-only verification."
          echo "Live provider authentication was subsequently verified after restoring ShopID 1258890."
          echo "No credentials, provider calls, payments, or production mutations are performed by this retired workflow."
