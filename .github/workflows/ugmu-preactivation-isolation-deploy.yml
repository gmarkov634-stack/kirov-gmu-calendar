name: UGMU preactivation isolation deploy (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve historical deployment boundary
        run: |
          echo "This one-time production isolation deployment was completed by run 32421951549."
          echo "The workflow is intentionally retired to prevent an accidental second production image mutation."
          echo "Use a new explicit fail-closed deployment boundary for later UGMU backend revisions."
