name: One-time verify floating-time API image

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/one-time-verify-floating-time-image-20260810.yml"

permissions:
  contents: write

jobs:
  verify:
    if: github.event.head_commit.message == 'Verify floating-time API image in Cloud.ru registry'
    runs-on: ubuntu-latest
    env:
      IMAGE: kgmu-calendar-api.cr.cloud.ru/api:1e539eece8e8a139aa37ec516b845f0d4ccc24b6
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: kgmu-calendar-api.cr.cloud.ru
          username: ${{ secrets.EVO_CR_LOGIN }}
          password: ${{ secrets.EVO_CR_PWD }}
      - name: Wait for immutable image tag
        run: |
          set -euo pipefail
          ok=0
          for attempt in $(seq 1 24); do
            if docker manifest inspect "$IMAGE" > /tmp/manifest.json 2>/dev/null; then
              ok=1
              break
            fi
            sleep 10
          done
          if [ "$ok" != 1 ]; then
            echo "Image tag did not appear: $IMAGE" >&2
            exit 2
          fi
          digest="$(sha256sum /tmp/manifest.json | awk '{print $1}')"
          mkdir -p ops/reports
          printf '{"version":1,"operation":"verify-floating-time-api-image","image":"%s","available":true,"manifestSha256":"%s"}\n' "$IMAGE" "$digest" > ops/reports/floating-time-api-image-20260810.json
      - name: Record verification
        if: always()
        env:
          JOB_STATUS: ${{ job.status }}
        run: |
          mkdir -p ops/reports
          if [ ! -f ops/reports/floating-time-api-image-20260810.json ]; then
            printf '{"version":1,"operation":"verify-floating-time-api-image","image":"%s","available":false}\n' "$IMAGE" > ops/reports/floating-time-api-image-20260810.json
          fi
          python - <<'PY'
          import json, os
          from pathlib import Path
          p=Path('ops/reports/floating-time-api-image-20260810.json')
          d=json.loads(p.read_text()); d['jobStatus']=os.environ['JOB_STATUS']
          p.write_text(json.dumps(d,ensure_ascii=False,indent=2)+'\n')
          PY
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ops/reports/floating-time-api-image-20260810.json
          git commit -m "Record floating-time API image verification" || true
          git push
