name: Probe pediatrics 131-140 official source

on:
  push:
    branches:
      - feat/pediatrics-131-140-2026-27-s1
    paths:
      - 'fixtures/tools/probe_pediatrics_131_140_source.py'
      - '.github/workflows/probe-pediatrics-131-140.yml'
  pull_request:
    branches: [main]
    paths:
      - 'fixtures/tools/probe_pediatrics_131_140_source.py'
      - '.github/workflows/probe-pediatrics-131-140.yml'

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check openpyxl==3.1.5
      - name: Fetch and mechanically dump official workbook
        run: python fixtures/tools/probe_pediatrics_131_140_source.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pediatrics-131-140-source-probe
          path: pediatrics-131-140-source-probe.json
          if-no-files-found: error
