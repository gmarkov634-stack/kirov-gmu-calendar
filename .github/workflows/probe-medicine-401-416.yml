name: Probe medicine 401-416 official source

on:
  push:
    branches:
      - feat/medicine-course-4-normalization
    paths:
      - 'fixtures/tools/probe_medicine_401_416_source.py'
      - '.github/workflows/probe-medicine-401-416.yml'
  pull_request:
    branches: [main]
    paths:
      - 'fixtures/tools/probe_medicine_401_416_source.py'
      - '.github/workflows/probe-medicine-401-416.yml'

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check openpyxl==3.1.5
      - name: Fetch and mechanically dump official workbook
        run: python fixtures/tools/probe_medicine_401_416_source.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: medicine-401-416-source-probe
          path: medicine-401-416-source-probe.json
          if-no-files-found: error
