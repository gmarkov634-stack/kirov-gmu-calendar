name: UGMU Cloud secret env shape (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Preserve completed prelaunch secret-shape boundary
        run: |
          echo "The prelaunch Cloud.ru/YooKassa secret-shape verifier is retired after UGMU launch."
          echo "It required UGMU_SALES_ENABLED=false and therefore no longer represents the production invariant."
          echo "The live secret reference and live provider mode were verified read-only after restoring ShopID 1258890."
          echo "No Cloud.ru credential, Secret Manager access, provider request, payment, or production mutation is performed here."
